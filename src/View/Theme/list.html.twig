<div class="col-4 d-flex justify-content-center align-items-center bg-green">
	<ul class="list-group list-unstyled">
		{% for theme in themes %}
			<li class="border-bottom">
				<a href="/subject/list?theme_id={{theme.id}}" class="bg-green text-color-clair fs-3 p-4 home-list-group-item list-group-item">
					{{theme.name}}
				</a>
			</li>
		{% endfor %}
	</ul>
</div>
